# Test samples

Place a Hülsbeck TFMX pair here for host tests:

    tests/samples/user-song.tfx
    tests/samples/user-song.sam

These rips are copyrighted and must **not** be committed or uploaded.
`/usr/bin/make test` looks for `user-song.tfx` + sibling `.sam`.

